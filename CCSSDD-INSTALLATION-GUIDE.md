# CCSSDD（Claude Code Spec-Driven Development）
## インストール & 初期設定チェックリスト（日本語）

---

## 対象

| 項目 | 内容 |
|------|------|
| IDE | VS Code |
| AI | Claude Code |
| 方法論 | Spec-Driven Development（CCSSDD） |

### IDE について（重要）

**CCSSDD は IDE に依存しない**。以下のどの IDE でも使用可能：

| IDE | 対応状況 | 備考 |
|-----|----------|------|
| **VS Code** | ✅ 推奨 | 本ガイドの対象 |
| Cursor | ✅ 可能 | AI 機能が重複するため非推奨 |
| Kiro | ✅ 可能 | Spec 管理機能が重複 |
| JetBrains系 | ✅ 可能 | IntelliJ, WebStorm など |
| Vim/Neovim | ✅ 可能 | ターミナルベース |

> **本ガイドでは VS Code を使用**
> 顧客環境の標準 IDE として VS Code を採用しているため

❌ **Cursor 不使用**（本環境では）

---

## 1. 前提条件チェック

- [ ] VS Code がインストール済み
- [ ] Node.js（LTS 推奨）がインストール済み
- [ ] Git が利用可能
- [ ] Claude Code を利用できる環境（認証済み）

### 1.1 VS Code インストール

**公式サイト**: https://code.visualstudio.com/

| OS | ダウンロード |
|----|-------------|
| Windows | https://code.visualstudio.com/download |
| macOS | https://code.visualstudio.com/download |
| Linux | https://code.visualstudio.com/download |

```bash
# macOS (Homebrew)
brew install --cask visual-studio-code

# Ubuntu/Debian
sudo apt update
sudo apt install code

# Windows (winget)
winget install Microsoft.VisualStudioCode
```

### 1.2 Node.js インストール

**公式サイト**: https://nodejs.org/

```bash
# macOS (Homebrew)
brew install node

# Ubuntu/Debian (NodeSource)
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs

# Windows (winget)
winget install OpenJS.NodeJS.LTS

# バージョン確認
node --version
npm --version
```

### 1.3 Git インストール

**公式サイト**: https://git-scm.com/

```bash
# macOS (Homebrew)
brew install git

# Ubuntu/Debian
sudo apt install git

# Windows (winget)
winget install Git.Git

# バージョン確認
git --version
```

---

## 2. Claude Code CLI のインストール

### 2.1 Claude Code インストール

```bash
npm install -g @anthropic-ai/claude-code
```

- [ ] `claude` コマンドが実行できることを確認

```bash
claude --version
```

---

## 3. プロジェクト初期化（CCSSDD 最小構成）

### 3.1 リポジトリ直下で実行

```bash
mkdir -p .claude/commands
touch CLAUDE.md
```

- [ ] `CLAUDE.md` が存在する
- [ ] `.claude/commands/` フォルダが存在する

---

## 4. CLAUDE.md（日本語・最小テンプレ）

👉 **この内容をそのまま貼ってOK**

```markdown
# Claude Code Instructions (日本語)

## 基本方針
- 本プロジェクトは Spec-Driven Development（SDD）を採用する
- コードより Spec を最優先とする
- Spec が存在しない実装は禁止

## 作業ルール
1. 必ず以下の順で進める
   - Requirements
   - Design
   - Tasks
   - Implementation
2. 曖昧な表現は禁止
3. Acceptance Criteria を満たさない実装は禁止

## Claude Code への指示方法
- 常に Spec を入力として与えること
- 勝手な仕様追加・変更は禁止
- 不明点は実装せず質問すること

## 出力ルール
- 日本語で回答
- コードには簡潔なコメントを付ける
```

- [ ] 日本語で明示されている
- [ ] 「Specがない実装は禁止」と書かれている

---

## 5. CCSSDD 用 Claude Command 作成

### 5.1 Spec 作成用コマンド

**`.claude/commands/spec.md`**

```markdown
以下の内容を Spec-Driven Development 形式で作成してください。

必須構成：
1. Requirements
   - Scope
   - Context
   - Requirements
   - Acceptance Criteria
2. Design
   - アーキテクチャ方針
   - データ構造
   - API / IF
3. Tasks
   - 実装タスク
   - Done 条件

注意：
- 曖昧な表現は禁止
- 実装せず Spec のみ出力
- 日本語で記載
```

- [ ] `spec` コマンドが存在する

### 5.2 実装用コマンド（Spec前提）

**`.claude/commands/implement.md`**

```markdown
以下の Spec に厳密に従って実装してください。

ルール：
- Spec に記載のない処理は実装しない
- Acceptance Criteria を必ず満たす
- 仕様が曖昧な場合は質問する
- 日本語コメントを付ける
```

---

## 6. VS Code での実行フロー（CCSSDD）

### 6.1 Spec 作成

```bash
claude spec
```

- [ ] Requirements / Design / Tasks が揃っている
- [ ] Acceptance Criteria が明確

### 6.2 実装

```bash
claude implement
```

- [ ] Spec を入力として渡している
- [ ] 生成コードを人がレビューしている

---

## 7. CCSSDD 実行ルール（重要）

- [ ] Specなしで `claude implement` を実行しない
- [ ] 仕様変更時は「先に Spec を修正」
- [ ] コード修正のみは禁止
- [ ] Spec とコードの乖離を放置しない

---

## 8. 日本向けチーム運用チェック

- [ ] Spec 承認者はローカル管理責任者
- [ ] 水平チームは Spec レビューのみ
- [ ] 実装責任は縦チーム
- [ ] 口頭合意は禁止（Spec必須）

---

## 9. やらないこと（再確認）

| ❌ やらない |
|-------------|
| Cursor 設定 |
| IDE 細かいカスタマイズ説明 |
| AI比較・ツール論 |
| Specなしの PoC 実装 |

---

## 10. Audit Hooks（監査ログ設定）

Claude Code を企業で使う場合、**ユーザー入力・コマンド実行を監査ログに残す**ことが重要。

### 10.1 settings.json の設定

**`.claude/settings.json`**

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/audit-user-prompt.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/audit-bash-command.sh"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/audit-file-changes.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/audit-tool-completion.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/audit-session-stop.sh"
          }
        ]
      }
    ]
  }
}
```

### 10.2 Hook イベント一覧

| Hook Event | 発火タイミング | 用途 |
|------------|----------------|------|
| `UserPromptSubmit` | ユーザーがプロンプト送信時 | **ユーザー入力の監査** |
| `PreToolUse` | ツール実行前 | コマンド・ファイル変更の監査 |
| `PostToolUse` | ツール実行後 | 結果ログ |
| `Stop` | Claude 応答終了時 | セッション終了ログ |

### 10.3 監査スクリプト例

**`.claude/hooks/audit-user-prompt.sh`**

```bash
#!/bin/bash
# ユーザープロンプトを JSONL 形式でログ

AUDIT_DIR="${HOME}/.claude/audit"
AUDIT_FILE="${AUDIT_DIR}/user-prompts.jsonl"

mkdir -p "$AUDIT_DIR"

PAYLOAD=$(cat)

echo "$PAYLOAD" | jq -c '
{
  timestamp: (now | todateiso8601),
  event: "UserPromptSubmit",
  session_id: .session_id,
  user: env.USER,
  hostname: env.HOSTNAME,
  cwd: .cwd,
  prompt: .prompt_text
}' >> "$AUDIT_FILE"

exit 0
```

### 10.4 Exit Code の意味

| Code | 動作 |
|------|------|
| `0` | 処理を続行 |
| `2` | 処理をブロック（実行禁止） |
| `1` | 非ブロッキングエラー |

### 10.5 監査ログの確認

```bash
# ユーザープロンプト履歴
tail -f ~/.claude/audit/user-prompts.jsonl | jq

# Bash コマンド履歴
cat ~/.claude/audit/bash-commands.jsonl | jq '.command'

# セッション別カウント
cat ~/.claude/audit/user-prompts.jsonl | jq -r '.session_id' | sort | uniq -c
```

- [ ] `.claude/settings.json` が存在する
- [ ] `.claude/hooks/` に監査スクリプトがある
- [ ] 監査ログが `~/.claude/audit/` に出力される

---

## 11. Developer Todo Lists（タスク管理）

Claude Code は **TodoWrite ツール** でタスク管理ができる。関数作成などの実装タスクを細分化して追跡可能。

### 11.1 CLAUDE.md に Todo ルールを追加

```markdown
## タスク管理ルール

### 機能実装時の Todo 作成
新機能を実装する際は、必ず以下のタスクを Todo として作成すること：

1. Spec 確認・理解
2. 関数シグネチャ定義
3. 入力バリデーション実装
4. メインロジック実装
5. エラーハンドリング実装
6. ユニットテスト作成
7. 統合テスト確認
8. コードレビュー依頼

### Todo 命名規則
- 日本語で記載
- 動詞で始める（例：「作成する」「確認する」「実装する」）
- Done 条件を明確に
```

### 11.2 関数作成時の Todo テンプレート

```markdown
## 関数作成 Todo（テンプレート）

### 対象関数: [関数名]

- [ ] Spec に定義された要件を確認
- [ ] 関数シグネチャ（引数・戻り値）を定義
- [ ] 入力値のバリデーションを実装
- [ ] メインロジックを実装
- [ ] エッジケースの処理を実装
- [ ] エラーハンドリングを実装
- [ ] JSDoc / コメントを追加
- [ ] ユニットテストを作成
- [ ] テストが Pass することを確認
- [ ] Spec の Acceptance Criteria を満たすことを確認
```

### 11.3 Claude Code での Todo 活用方法

```bash
# Spec と Todo を一緒に渡す
claude implement "以下の Spec に従って実装してください。
各ステップを Todo として管理し、完了したらチェックしてください。"
```

- [ ] CLAUDE.md に Todo ルールが記載されている
- [ ] 関数作成時に Todo テンプレートを使用している

---

## 12. --dangerously-skip-permissions モード

### 12.1 概要

`--dangerously-skip-permissions` は **全ての確認をスキップ** するモード。
効率は上がるが、**リスクも増大**する。

```bash
# 危険モードで起動
claude --dangerously-skip-permissions
```

### 12.2 リスクと対策

| リスク | 対策 |
|--------|------|
| 意図しないファイル削除 | Audit Hook で全操作をログ |
| 危険なコマンド実行 | PreToolUse Hook でブロック |
| 機密情報の漏洩 | `.gitignore` 徹底、環境変数で管理 |
| 本番環境への影響 | 開発環境でのみ使用 |

### 12.3 危険モード使用時の必須設定

**1. Audit Hook を必ず有効化**

```json
{
  "hooks": {
    "UserPromptSubmit": [...],
    "PreToolUse": [...],
    "PostToolUse": [...]
  }
}
```

**2. 危険コマンドブロック Hook**

**`.claude/hooks/block-dangerous.sh`**

```bash
#!/bin/bash
PAYLOAD=$(cat)
COMMAND=$(echo "$PAYLOAD" | jq -r '.tool_input.command')

# ブロックするコマンドパターン
if echo "$COMMAND" | grep -qE 'rm -rf /|sudo|chmod 777|curl.*\|.*sh|:(){ :|:& };:'; then
  echo "BLOCKED: $COMMAND" >> ~/.claude/audit/blocked.log
  echo "危険なコマンドをブロックしました: $COMMAND" >&2
  exit 2  # ブロック
fi

exit 0
```

**3. CLAUDE.md に危険モードルールを追加**

```markdown
## 危険モード使用ルール

### 許可される操作
- ファイル作成・編集
- npm / yarn コマンド
- git コマンド
- テスト実行

### 禁止される操作
- rm -rf（ルート）
- sudo コマンド
- chmod 777
- curl | sh パターン
- 本番環境への接続
```

### 12.4 危険モード使用時のチェックリスト

- [ ] Audit Hook が有効
- [ ] 危険コマンドブロック Hook が有効
- [ ] `.gitignore` に機密ファイルが含まれている
- [ ] 開発環境でのみ使用
- [ ] 本番接続情報が環境に存在しない
- [ ] チーム全員がリスクを理解している

### 12.5 推奨: 段階的アプローチ

```
1. 最初は通常モードで作業
2. 確認が多すぎる場合のみ危険モードを検討
3. 危険モード使用時は必ず Audit ログを確認
4. 問題があれば即座に通常モードに戻る
```

---

## 13. 不要なフォルダ・ファイルの整理（重要）

### 結論（はっきり）

CCSSDD を「VS Code + Claude Code」だけで使う場合、
**Kiro 用・Cursor 用フォルダは一切不要です。**

### 作ってはいけない／消してよいフォルダ

| ❌ 不要 | 理由 |
|---------|------|
| `.kiro/` | Kiro 専用の Spec 管理・Agent 機構 |
| `.kiro/specs` | Kiro 専用 |
| `.kiro/steering` | Kiro 専用 |
| `.kiro/agents` | Kiro 専用 |
| `.cursorrules` | Cursor IDE 固有 |
| `specs/` | Cursor の conventions |

### なぜ Kiro / Cursor フォルダが不要なのか

| 項目 | 理由 |
|------|------|
| `.kiro/*` | Kiro 専用の Spec 管理・Agent 機構。Claude Code では不要 |
| `.cursorrules` | Cursor IDE 固有 |
| `specs/` | Cursor の conventions。Claude Code では任意 |
| 複数AI設定 | CCSSDD では Claudeのみ |

👉 **入れると逆に混乱する**

### よくある間違い（避ける）

| ⚠️ 間違い | 結論 |
|-----------|------|
| 「一応置いておく」 | ❌ 不要 |
| 「将来使うかもしれない」 | ❌ 不要 |
| 「サンプルにあったから」 | ❌ 不要 |

### 正しい判断基準（迷ったらこれ）

> **それは Claude Code が直接使うか？**
> - Yes → 残す
> - No → 削除

### 既に入っている場合の整理コマンド

```bash
rm -rf .kiro
rm -f .cursorrules
rm -rf specs
```

---

## 14. CCSSDD の本質（ツール非依存）

```
CCSSDD = Claude Code × Spec-Driven Development
```

- CCSSDD は「フォルダ構造」や「IDE」に依存しない
- Kiro / Cursor は **補助ツール**
- **Spec の中身と運用ルールが全て**

---

## 15. Kiro 機能を Claude Code で実現する方法

Kiro の構造は使わないが、**同等の機能は Claude Code で実現可能**。

### 機能マッピング表

| Kiro 構造 | 目的 | Claude Code 相当 |
|-----------|------|------------------|
| `.kiro/specs/` | Spec 管理 | `docs/specs/` または任意フォルダ |
| `.kiro/steering/` | AI への指示・ルール | `CLAUDE.md` |
| `.kiro/agents/` | カスタム Agent | `.claude/commands/` |

### 15.1 Spec 管理（`.kiro/specs/` 相当）

Kiro は `.kiro/specs/` に Spec を格納するが、Claude Code では **任意のフォルダで OK**。

```
docs/
└─ specs/
   ├─ feature-login-spec.md
   ├─ feature-payment-spec.md
   └─ api-design-spec.md
```

👉 **重要**: Spec を読ませる時は `@docs/specs/xxx.md` で参照

### 15.2 Steering（`.kiro/steering/` 相当）

Kiro は `.kiro/steering/` でプロジェクトルールを定義するが、Claude Code では **`CLAUDE.md` が全て担う**。

```markdown
# CLAUDE.md に書く内容（steering 相当）

## プロジェクト概要
- 目的・背景

## 技術スタック
- 言語、フレームワーク

## コーディング規約
- 命名規則
- ファイル構成

## 禁止事項
- やってはいけないこと
```

### 15.3 Agents（`.kiro/agents/` 相当）

Kiro は `.kiro/agents/` でカスタム Agent を定義するが、Claude Code では **`.claude/commands/` が同等機能**。

**例: レビュー用 Agent**

`.claude/commands/review.md`
```markdown
以下のコードをレビューしてください。

チェック項目：
- Spec との整合性
- セキュリティ問題
- パフォーマンス
- 可読性

出力：
- 問題点リスト
- 修正提案
- 日本語で回答
```

**例: テスト生成 Agent**

`.claude/commands/test.md`
```markdown
以下のコードに対するユニットテストを作成してください。

ルール：
- 境界値テスト必須
- エラーケース網羅
- テストフレームワーク: [プロジェクトに合わせて指定]
```

### 15.4 完全マッピング構成（推奨）

```
repo-root/
├─ CLAUDE.md                    ← steering 相当
├─ .claude/
│  └─ commands/
│     ├─ spec.md                ← Spec 生成 Agent
│     ├─ implement.md           ← 実装 Agent
│     ├─ review.md              ← レビュー Agent
│     └─ test.md                ← テスト Agent
├─ docs/
│  └─ specs/                    ← .kiro/specs/ 相当
│     └─ feature-x-spec.md
└─ src/
```

---

## 16. VS Code + Claude Code 専用の最小構成（推奨）

```
repo-root/
├─ CLAUDE.md              ← 必須（日本語ルール）
├─ .claude/
│  └─ commands/
│     ├─ spec.md          ← Spec生成用
│     └─ implement.md     ← 実装用
├─ docs/                  ← Spec置き場（任意だが推奨）
│  └─ feature-x-spec.md
└─ src/                   ← アプリコード
```

### ✔ 必須なのはこれだけ

- `CLAUDE.md`
- `.claude/commands/`

---

## 17. 最終 Go / No-Go チェック

- [ ] VS Code のみで完結
- [ ] Claude Code は Spec 前提
- [ ] CCSSDD の3点セットが存在
- [ ] 日本語 Spec で統一されている

---

## ✅ 結論

```
CCSSDD = VS Code + Claude Code + 日本語Spec
```

**余計なIDEや設定を増やさず、**
**仕様を起点に、**
**人が判断し、Claudeが加速する。**

---

## 運用ルール（最終まとめ）

> **CCSSDD を Claude Code で使うなら**
> **Kiro / Cursor の痕跡は一切残さない**

- Spec は Markdown
- 運用ルールは CLAUDE.md
- 実装は Spec First
